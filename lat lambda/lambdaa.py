# students = [("Emil", 25), ("Tobias", 22), ("Linus", 24)]
# sorted_students = sorted(students, key=lambda x: x[1])
# print(sorted_students)

# def myfunc(n):
#     return lambda a : a * n
# mymultiplier = myfunc(2)
# print(mymultiplier(4))

# bilangan = [1, 2, 3, 4, 5, 6]
# ganjil = list(filter(lambda x: x % 2 == 1, bilangan))
# print(ganjil)

# number = [1,3,5,7]
# kuadrat = list(map(lambda x: x ** 2, number))
# print(kuadrat)

# buah = ["markisa", "nanas", "pir"]
# sorted_buah = sorted(buah, key=lambda x: len(x))
# print(sorted_buah)